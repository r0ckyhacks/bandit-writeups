The password for the next level is stored somewhere on the server and has all of the following properties:
	- owned by user bandit7
	- owned by group bandit6
	- 33 bytes in size
	Use the find command with those properties to get the required file, cat the file and get the password to next level entry.
	commands:
			"find . -user bandit7 -group bandit6 -size 33c -type f 2>/dev/null"     -----> to find the file that is 1033 bytes in size
			"ssh bandit7@bandit.labs.overthewire.org -p2220"
