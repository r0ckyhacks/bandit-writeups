The password for the next level is stored in the file data.txt and is the only line of text that occurs only once, to extract the unique text from a file we use below commands.
	Command:
			"sort data.txt | uniq -u"      -------> first sorts the data in it and shows the line that appears only once.
			"ssh bandit9@bandit.labs.overthewire.org -p2220"
